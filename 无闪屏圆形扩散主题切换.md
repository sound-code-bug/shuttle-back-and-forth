# 无闪屏圆形扩散主题切换（jQuery / 原生 / Vue / React 通用思路）

你的这个方案，本质上是：

> “先截取旧页面 → 真正切换主题 → 用遮罩动画显示新旧主题交替”

这是目前很多高质量主题切换动画的核心思路。

例如：

* 明亮 → 黑暗：旧页面缩小消失
* 黑暗 → 明亮：旧页面向外扩散

这种方案最大的优点：

* 无闪屏
* 不依赖 ViewTransition API
* 兼容性高
* Vue / React / Next 都能复用

---

# 一、最终效果原理

整个流程：

```text
点击按钮
   ↓
对当前页面截图（旧主题）
   ↓
把截图盖在页面最上层
   ↓
真正切换 body class
   ↓
新主题已经渲染完成
   ↓
通过 clip-path / mask 动画
逐渐移除旧主题截图
   ↓
形成圆形扩散切换
```

---

# 二、核心技术拆解

你的代码核心只有 3 个部分：

---

# 1. DOM 截图

```js
function createPageSnapshot() {

  const html =
    new XMLSerializer()
      .serializeToString(document.documentElement)

  const svg =
    `
    <svg xmlns="http://www.w3.org/2000/svg"
         width="${window.innerWidth}"
         height="${window.innerHeight}">
      <foreignObject
        width="100%"
        height="100%">
        ${html}
      </foreignObject>
    </svg>
    `

  return `data:image/svg+xml;charset=utf-8,${encodeURIComponent(svg)}`
}
```

---

## 原理

这里不是“真正截图”。

而是：

```text
DOM
↓
序列化成 HTML 字符串
↓
塞进 SVG foreignObject
↓
转成 dataURL
↓
作为 background-image
```

最终得到：

```css
background-image: url(data:image/svg+xml...)
```

---

## 为什么这样做？

因为：

```text
主题切换时 DOM 会立刻变化
```

如果不提前保存旧页面：

```text
切换瞬间：
旧主题消失
新主题出现
```

用户就会看到：

# 闪屏

所以：

```text
必须先保存旧页面
```

然后：

```text
让旧页面“假装还在”
```

再做动画。

---

# 三、主题层（最关键）

```css
.theme-layer {
  position: fixed;
  inset: 0;

  z-index: 9999;

  pointer-events: none;

  background-size: cover;
  background-position: center;
}
```

---

## 作用

这是：

# “旧主题截图层”

它覆盖整个页面：

```text
用户看到的：
其实不是旧页面

而是：
旧页面截图
```

真正的 DOM 已经切换成新主题了。

---

# 四、真正切换主题

```js
dark = !dark

$("body")
  .toggleClass("dark", dark)
  .toggleClass("light", !dark)
```

这里：

```text
真正发生了主题切换
```

但是：

由于旧截图还盖在最上层：

```text
用户看不见变化
```

所以：

# 完全不会闪

---

# 五、为什么动画不会闪屏？

因为：

```text
动画操作的是：
截图层

不是：
真实 DOM
```

真实 DOM：

```text
已经切换完成
```

所以：

```text
动画期间
页面不会重新渲染
```

这就是无闪屏核心。

---

# 六、明亮 → 黑暗

---

## 视觉逻辑

```text
黑暗主题已经存在
```

但是：

```text
旧截图（明亮）盖在上面
```

所以：

只需要：

# 让旧截图缩小

即可露出黑暗主题。

---

## 实现

```js
clipPath:
  `circle(${radius}px at ${x}px ${y}px)`
```

---

## 原理

```text
clip-path:
决定元素哪里可见
```

例如：

```css
clip-path: circle(100px at right top);
```

表示：

```text
只有圆内可见
```

所以：

```text
圆越来越小
```

旧主题就消失了。

---

# 七、黑暗 → 明亮

这里逻辑不同。

---

## 当前状态

```text
真实 DOM：
已经是明亮主题

截图层：
还是黑暗主题
```

所以：

你不能再用 clip-path。

因为：

```text
clip-path 是保留圆内
```

而你现在需要：

```text
保留圆外
```

因此：

你用了：

# mask

---

# 八、mask 原理

```js
const mask =
  `radial-gradient(
    circle at ${x}px ${y}px,
    transparent ${radius}px,
    black ${radius + 1}px
  )`
```

---

## 作用

```text
透明区域：
看不到截图层

黑色区域：
能看到截图层
```

因此：

```text
圆内：
透明
→ 显示新主题

圆外：
还是旧截图
```

最终形成：

# 新主题向外扩散

---

# 九、为什么要强制重绘？

你代码里的：

```js
oldLayer[0].offsetHeight
```

非常关键。

---

## 作用

强制浏览器：

# 立即渲染当前状态

否则：

浏览器可能把：

```text
创建元素
设置样式
开始动画
```

优化合并。

结果：

```text
动画直接跳最终状态
```

---

## 所以必须：

```js
读取 offsetHeight
```

强制浏览器：

```text
“先渲染一次”
```

---

# 十、jQuery animate 为什么能用？

因为：

```js
$({ value: 0 }).animate(...)
```

本质是：

# 数值补间动画

jQuery 会：

```text
0 → 1000
```

逐帧计算。

然后：

```js
step(value) {}
```

拿到每一帧的值。

---

# 十一、为什么不用 CSS transition？

因为：

```text
clip-path
mask
```

动态计算比较复杂。

尤其：

```text
mask 渐变
```

CSS transition 很难控制。

所以：

JS 驱动更灵活。

---

# 十二、如果是原生 JS 怎么写？

核心替换：

---

## jQuery

```js
$("#toggle").on("click", fn)
```

替换：

```js
document
  .getElementById("toggle")
  .addEventListener("click", fn)
```

---

## jQuery css

```js
oldLayer.css({
  clipPath: xxx
})
```

替换：

```js
oldLayer.style.clipPath = xxx
```

---

## jQuery animate

换成：

```js
requestAnimationFrame
```

例如：

```js
function animate({
  from,
  to,
  duration,
  onUpdate,
  onComplete
}) {

  const start = performance.now()

  function frame(now) {

    const progress =
      Math.min(
        (now - start) / duration,
        1
      )

    const value =
      from + (to - from) * progress

    onUpdate(value)

    if (progress < 1) {

      requestAnimationFrame(frame)

    } else {

      onComplete?.()
    }
  }

  requestAnimationFrame(frame)
}
```

---

# 十三、Vue 中怎么做？

---

# Vue 核心思路

不要把动画逻辑写组件里。

应该：

# 封装成 composable

例如：

```text
composables/useThemeTransition.ts
```

---

## Vue 示例

```js
const toggleTheme = async () => {

  await startThemeTransition()

  dark.value = !dark.value
}
```

---

## 为什么？

因为：

```text
主题切换：
属于全局能力
```

不是某个组件的能力。

---

# 十四、React / Next 中怎么做？

---

# React 最佳实践

封装：

```text
hooks/useThemeTransition.ts
```

---

## 示例

```tsx
const { toggleTheme } =
  useThemeTransition()

<button onClick={toggleTheme}>
  切换主题
</button>
```

---

# Next.js 重点

Next 有：

# SSR

因此：

```text
不能直接操作 document
```

必须：

```tsx
useEffect(() => {})
```

后执行。

否则：

```text
Hydration Error
```

---

# 十五、为什么很多项目会闪屏？

因为：

他们这样写：

```js
document.body.classList.toggle("dark")
```

问题：

```text
DOM 立即重新渲染
```

用户会看到：

```text
白 → 黑
```

瞬间变化。

尤其：

# 大页面

更明显。

---

# 十六、这个方案的优点

---

## 1. 无闪屏

因为：

```text
动画操作的是截图
```

---

## 2. 不依赖框架

适用于：

* 原生
* Vue
* React
* Next
* Nuxt
* Electron

---

## 3. 可自定义动画

可以改成：

* 波纹
* 扩散
* 百叶窗
* 模糊
* 液态
* 像素化

---

## 4. 性能非常高

因为：

```text
动画期间：
真实 DOM 不变化
```

只有：

```text
一个 fixed 图层
```

在变化。

---

# 十七、这个方案的缺点

---

## 1. 无法截图视频

foreignObject 有限制。

---

## 2. 超大 DOM 时

生成 SVG 会慢。

---

## 3. 某些复杂 CSS

可能无法完整序列化。

例如：

* filter
* iframe
* canvas

---

# 十八、更现代的方案

其实现在浏览器有：

# View Transition API

例如：

```js
document.startViewTransition()
```

但：

兼容性还不够。

所以：

你这种方案：

# 目前是最稳的跨框架方案

---

# 十九、推荐最终项目结构

---

## React

```text
src/
 ├── hooks/
 │    └── useThemeTransition.ts
 │
 ├── utils/
 │    ├── snapshot.ts
 │    ├── animate.ts
 │    └── transition.ts
```

---

## Vue

```text
src/
 ├── composables/
 │    └── useThemeTransition.ts
 │
 ├── utils/
 │    ├── snapshot.ts
 │    ├── animate.ts
 │    └── transition.ts
```

---

# 二十、最终总结

你的方案本质：

```text
旧页面截图
+
新主题提前渲染
+
遮罩动画
```

核心思想：

```text
动画操作截图
而不是 DOM
```

这就是：

# 高级主题切换动画的真正原理。
